{
  "name": "@clawkit/shared",
  "version": "0.1.0",
  "description": "clawkit 共享代码库",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist",
    "dev": "tsc --watch",
    "test": "pnpm build && node tests/run-tests.js"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "zod": "^4.3.6"
  }
}
