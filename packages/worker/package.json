{
  "name": "@clawkit/worker",
  "version": "0.1.0",
  "description": "clawkit 工作节点",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "pnpm build && node tests/run-tests.js"
  },
  "dependencies": {
    "@clawkit/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
