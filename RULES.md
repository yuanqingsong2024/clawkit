# clawkit 项目规则

本文档定义了 clawkit 项目的核心规则和约束。

## 核心原则

### 1. 最小可用原则
- 每个阶段只实现明确定义的功能
- 不提前实现未来可能需要的功能
- 保持代码简单直接

### 2. 渐进式开发
- 从简单到复杂
- 从核心到外围
- 每个阶段都可用

### 3. 质量优先
- 代码质量高于开发速度
- 文档与代码同步
- 充分测试后再发布

## 技术约束

### TypeScript 规范
- 必须使用严格模式（strict: true）
- 禁止使用 `any`，必要时使用 `unknown`
- 所有函数必须有明确的返回类型
- 优先使用接口（interface）而非类型别名（type）

### 代码规范
- 单个函数不超过 50 行
- 单个文件不超过 300 行
- 避免深层嵌套（最多 3 层）
- 避免复杂的设计模式

### 依赖管理
- 最小化外部依赖
- 优先使用 Node.js 内置模块
- 新增依赖需要评估必要性和维护成本
- 使用 `workspace:*` 引用内部包

## 开发流程

### 功能开发
1. 明确需求和验收标准
2. 设计简单方案
3. 小步实现和提交
4. 测试验证
5. 更新文档

### 代码审查
- 是否符合当前阶段目标
- 是否引入不必要的复杂度
- 是否有充分的注释
- 是否更新了文档

## 禁止事项

### 开发禁止
- ❌ 一次性实现多个功能
- ❌ 引入未讨论的依赖
- ❌ 修改不相关的代码
- ❌ 删除现有功能（除非明确要求）
- ❌ 过度设计和过度抽象

### 代码禁止
- ❌ 使用 `any` 类型
- ❌ 空的 catch 块
- ❌ 硬编码配置
- ❌ 未处理的 Promise
- ❌ 删除测试以通过构建

## 文档要求

### 必须文档
- 每个公共 API 必须有注释
- 每个 package 必须有 README
- 架构变更必须更新文档
- 新功能必须有使用说明

### 注释规范
- 使用中文注释
- 解释"为什么"而不是"做什么"
- 复杂逻辑必须有注释
- 公共函数必须有 JSDoc

## 测试要求

### 测试原则
- 核心功能必须测试
- 边界情况必须覆盖
- 测试代码也要保持简单
- 测试失败必须修复，不能删除

### 测试范围
- 单元测试：核心逻辑
- 集成测试：组件交互
- 手动测试：用户场景

## 版本管理

### 提交规范
```
<type>(<scope>): <subject>

<body>
```

类型：
- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档更新
- `refactor`: 重构
- `test`: 测试相关
- `chore`: 构建、工具等

### 版本号规则
- 遵循语义化版本（Semantic Versioning）
- 0.x.x 为开发阶段
- 1.0.0 为第一个稳定版本

## 问题处理

### 遇到问题时
1. 先理解问题根源
2. 查看相关文档和代码
3. 提出解决方案并说明理由
4. 等待确认后再实施

### 技术债务
- 及时记录技术债务
- 定期评估和清理
- 不累积过多债务

## 安全要求

### 代码安全
- 不在代码中硬编码密钥
- 敏感信息使用环境变量
- 输入必须验证
- 错误信息不泄露敏感信息

### 依赖安全
- 定期检查依赖漏洞
- 及时更新安全补丁
- 避免使用废弃的包

## 性能要求

### 性能原则
- 先保证正确性，再优化性能
- 不做过早优化
- 性能优化必须有数据支撑
- 保持代码可读性

## 兼容性

### 支持范围
- Node.js 20+ LTS
- Linux、macOS
- 主流终端

### 不支持
- 旧版本 Node.js
- Windows（待定）
- 非标准环境

## 规则变更

### 何时修改规则
- 规则阻碍开发
- 发现更好的实践
- 团队达成共识

### 如何修改规则
1. 提出修改建议
2. 说明理由和影响
3. 讨论并达成共识
4. 更新文档
5. 通知所有人

## 总结

这些规则旨在：
- 保持代码质量
- 控制项目复杂度
- 提高开发效率
- 确保项目可维护

遵守规则，但不僵化。规则是为了更好地开发，而不是束缚。

---

## ClawKit 工程规则补充

### 1. 总原则

- 先完成最小可用版本
- 先完成初始化和骨架，不提前实现后续阶段逻辑
- 每次只解决一个阶段问题
- 任何新增复杂度都必须有明确理由

### 2. 文档规则

- 所有文档必须使用中文
- 文档优先说明"为什么这样设计"
- 设计文档应区分：当前已实现、计划实现、明确不做

### 3. 代码规则

- 所有注释使用中文
- 公共模块必须有明确职责说明
- 不允许在初始化阶段写大量占位业务代码
- 禁止复制粘贴式堆砌代码
- 共享类型优先放到 shared 包

### 4. 目录规则

- CLI、Controller、Worker、Shared、Templates 分层清晰
- 配置样例统一放在 examples
- 设计文档统一放在 docs
- 可复用技能统一放在 skills
- OpenCode 自定义命令统一放在 .opencode/commands

### 5. 提交规则

- 提交信息尽量简洁明确
- 初始化阶段建议使用：
  - `chore: 初始化项目骨架`
  - `docs: 补充架构与范围文档`
  - `build: 调整 workspace 与构建配置`

### 6. 当前阶段边界

当前阶段禁止：
- Web UI
- 自动 PR
- 消息队列
- 多租户
- 复杂鉴权
- 真实派发逻辑
